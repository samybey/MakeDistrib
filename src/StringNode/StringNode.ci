module StringNode {

  chare StringNode {
    entry StringNode(std::string name,std::vector<std::string> dependencesVector,std::string command);
	
	entry StringNode();
	
	/// Entry Methods ///
	entry [sync] std::string getName();

	entry [sync] std::vector<std::string> getDependencesVector();

	entry [sync] std::string getCommand();

	entry [sync] bool deleteDependence(std::string dependence);

	entry void displayNode();
  };
};